    @model List<User>

    <li>
        @if (Model != null && Model.Count > 0)
        {
            foreach (User user in Model)
            {
                <p>Name: @user.UserName</p>
            }
        }
        else
        {
            <p>No users found.</p>
        }
    </li>
